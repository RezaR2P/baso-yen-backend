info:
  name: update
  type: http
  seq: 1

http:
  method: PUT
  url: http://localhost:3000/api/categories/5
  body:
    type: json
    data: |-
      {
        "name": "Ayam aja"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
