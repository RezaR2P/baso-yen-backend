info:
  name: updateStatus
  type: http
  seq: 1

http:
  method: PATCH
  url: http://localhost:3000/api/contacts/1/status
  body:
    type: json
    data: |
      {
        "status": "read"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​